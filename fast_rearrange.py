import os 

d = [1, 2, ,6, 34, 5, 7, 8, 9, 35]
#冒泡排序的python实现
def rearrange(self，d)：
	i = 0
	len = d.length()
	while i < len:
		for i in d:
			if d[i] < d[i + 1]:
				continue
			else d[i] > d[i - 1]:
				d[i] = m
				d[i] = d[i + 1]
				d[i + 1] = m	
		return d
	print "排序后的d的顺序为:" + d



	